# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.python-version

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Build / packaging artifacts
*.zip
*.tar.gz
dist/
build/
# Note: build/ is the default output of tools/generate_procurement_packet.py.
# Committed example output lives under generated/examples/.

# Tool / agent caches
.claude/
.cache/

# Local-only environment overrides
.env
.env.local
# Calendar event payloads are regenerated by `pipeline.py calendar` / `lead_radar.py calendar`;
# only the idempotency ledgers (_calendar_state.json) are tracked.
bids/active/_calendar_events.json
leads/review/_calendar_events.json
